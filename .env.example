# ClickUp OAuth Configuration
# Get these from: https://app.clickup.com/settings/apps

# OAuth Client ID (required)
CLICKUP_CLIENT_ID=your_client_id_here

# OAuth Client Secret (required)
CLICKUP_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI (required)
# Must match the redirect URI configured in your ClickUp app
CLICKUP_REDIRECT_URI=http://localhost:3000/callback

# ClickUp Workspace/Team ID (required)
# Find this in your workspace settings or API responses
CLICKUP_WORKSPACE_ID=your_workspace_id_here

# ClickUp Space ID (optional)
# If you want to work primarily in a specific space
CLICKUP_SPACE_ID=your_space_id_here

# Logging Level (optional)
# 0 = DEBUG, 1 = INFO, 2 = WARN, 3 = ERROR
LOG_LEVEL=1

# ============================================================================
# Instructions for Getting Your IDs:
# ============================================================================

# 1. CLIENT ID & SECRET:
#    - Go to: https://app.clickup.com/settings/apps
#    - Click "Create an App" or select existing app
#    - Copy Client ID and Client Secret

# 2. WORKSPACE_ID (Team ID):
#    - After OAuth authentication, call: GET /team
#    - Use the 'id' field from the response
#    - Or check your ClickUp URL: https://app.clickup.com/TEAM_ID/...

# 3. SPACE_ID:
#    - After OAuth authentication, call: GET /team/TEAM_ID/space
#    - Use the 'id' field for your desired space
#    - Or check your ClickUp URL when viewing a space

# 4. REDIRECT_URI:
#    - Must exactly match the Redirect URL(s) configured in your ClickUp app
#    - For local development: http://localhost:3000/callback
#    - For production: https://yourdomain.com/callback

# ============================================================================
# Security Notes:
# ============================================================================

# - NEVER commit this file with real credentials to version control
# - Add .env to your .gitignore (already configured)
# - Store production secrets in secure environment variable services
# - Rotate secrets regularly
# - Use different credentials for development/staging/production
